# Web specifications

UI flow specifications and acceptance criteria for `@devdigest/web` belong here. Keep component-level executable checks beside the components as `*.test.tsx`, and keep full browser journeys in [`../../e2e/specs`](../../e2e/specs/README.md).
